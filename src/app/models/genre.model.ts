import { Book } from "./book.model";

export class Genre {
  GenreId: number;
  Name: string;

  // constructor(genre: Genre) {
  //   Object.keys(genre).forEach((prop) => {
  //     this[prop] = genre[prop];
  // });
  /**
   *
   */
  constructor() {


  }
}
